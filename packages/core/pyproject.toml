[project]
name = "core"
version = "0.1.0"
description = "Shared runtime utilities for Talking Head Orchestrator services"
requires-python = ">=3.10"
dependencies = [
  "loguru>=0.7.2",
]

[build-system]
requires = ["uv_build"]
build-backend = "uv_build"
